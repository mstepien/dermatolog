import pytest

pytest.skip("Cleanup functionality removed as images are no longer stored on server.", allow_module_level=True)

import os
# ... rest of the file content commented out or ignored by skip above
